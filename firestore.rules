rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to comments and reactions
    match /comments/{commentId} {
      allow read, write: if true;
    }
    match /reactions/{reactionId} {
      allow read, write: if true;
    }
  }
}
